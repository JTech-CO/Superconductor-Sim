{
  "name": "superconductor-sim",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python -m http.server 8000",
    "build:compat": "node scripts/build-compat.mjs",
    "check": "node scripts/build-compat.mjs && node --check js/app.bundle.js && node --test tests/*.test.mjs"
  }
}
